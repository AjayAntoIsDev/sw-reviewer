# Task T01: Final Policy Schema

**Goal**: Define final policy schema (`required/advisory/manual_only`)

**Summary**:
Implemented the `PolicyCheck` and `PolicyLevel` schema using Pydantic in `src/sw_reviewer/models.py`. The levels include `required`, `advisory`, and `manual_only`. Fields include `id`, `category`, `level`, and `description`.
