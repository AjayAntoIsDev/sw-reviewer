# Next Steps post-T07–T10

- Add dedicated web integration test with a mock HTTP server.
- Extend auth flow detection to cover more OAuth providers.
- Add configurable flow steps (JSON-driven test sequences).
